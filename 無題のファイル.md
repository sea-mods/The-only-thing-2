# AHC069 Picnic — Beam Search + Monte Carlo Rollout 設計書

以下、そのまま実装に落とせるレベルで設計します。実装は C++17 を前提とします。

---

## 1. 全体アーキテクチャ

```
グループ i 到着
   │
   ├─ (a) 状態複製 → 候補生成 (Place各種 + Skip + 事前移動あり/なし)
   │        候補数 Cand ≈ 20〜40
   │
   ├─ (b) Beam Search 1手先展開
   │        静的評価で上位 K=4〜8 に絞る
   │
   ├─ (c) 各 Beam 候補について Monte Carlo Rollout を R=8〜16 回
   │        残り全グループを高速Greedyでシミュレート
   │
   ├─ (d) 平均最終スコア argmax の候補を採用
   │
   └─ (e) 実手として出力（移動列 + Yes/No/配置）
```

「本物の状態」は1本の系列として進み、Beam 部分は「i の 1 手」だけを幅広く探る **Flat Beam + MC評価** 構成にします。理由は 4 章で詳述。

---

## 2. 状態 (State) 設計

### 2.1 コア構造

```cpp
struct Group {
    int id;
    int S, T, P, V;
    int C_min_L;   // これまでに経験した最悪の L（Pは固定なのでLだけ持てば十分）
    int cur_L;     // 現在位置での L
    std::vector<int> cells; // 占有マス（idx = x*N + y）、サイズ P
};

struct State {
    // --- 静的情報（共有・const） ---
    // N, M, R, park[N*N]（芝生=0 / 池=1）は Env に持たせて参照

    // --- 動的情報 ---
    int8_t  owner[N*N];        // -1 = 空, それ以外は group id
    std::vector<Group> live;   // 現利用中グループ（IDでない配列インデックス）
    std::vector<int>   id2slot;// group id -> live index, -1 で不在
    long long money;           // 所持金（負可）
    int next_i;                // 次に到着するグループ番号
    // 退去イベント min-heap（T_j, group_id）— 退去時に即精算
    std::priority_queue<
        std::pair<int,int>,
        std::vector<std::pair<int,int>>,
        std::greater<>> leave_pq;

    // --- Rollout用軽量表現（後述） ---
};
```

**メモリのポイント**

- `owner` は 2500 バイト。`memcpy` で clone コスト ≈ 数百ns。
- `cells` は 4〜150 要素、`live.size()` は理論上最大数百。1 State ≒ 数十 KB。
- Beam K=8 + Rollout 並列 R=16 でも数 MB に収まる設計にする。

### 2.2 コンパクト度と境界長 L

`P` は固定なので `C = 4√P / L` の分子は前計算。`L` はマスごとに:

```
L(cell) = 4 - (領域内の上下左右隣接マス数)
```

領域全体では `L_total = 4P - 2*(内部隣接ペア数)`。  
配置時に一度計算しておき、以後は「その領域を変えるとき」のみ差分更新。

### 2.3 精算

退去時: `money += round(V * 4*sqrt(P) / C_min_L)`。  
`round(x) = floor(x + 0.5)`（負なし想定）。

---

## 3. 候補生成（1 グループぶんの手）

到着グループ i について次を列挙。

### 3.1 基本候補（配置形状のテンプレ）

`P = P_i` に対し、次の順で「なるべく正方形に近い長方形」の候補サイズを作る。

```
for h in [1..P]:
    w = ceil(P/h)
    if h*w - P <= h and h <= w+2:  // 「ほぼ長方形」を許容
        yield RectTemplate(h, w, P)  // 左上詰め + 余りマス位置は複数試す
```

各テンプレを全ての左上位置 (r,c) に置き、次を満たすものを候補セルとして採用:

- 全マスが芝生
- どれも `owner == -1`
- 4-連結（テンプレなので保証）

### 3.2 拡張候補

- **BFS成長**: シード (r,c) から幅優先で P マス取る。周長最小になる順で拡張。
- **穴回避**: L が小さい上位を選ぶ。
- **Skip 候補**: 配置しない。

### 3.3 事前移動を伴う候補（軽量）

- 「新規配置が置けなかったとき」または「置いても L が悪いとき」だけ検討。
- 対象: 現在利用中のうち **V が小さい & 移動コスト round(V*R) が非常に小さい** グループ 上位 3 つ程度。
- 各対象について「近くの隙間へ矩形再配置」を試みる。
- 効果が「見込み利用料増分 − 移動コスト」で正になるものだけ残す。

### 3.4 候補の 1 次スクリーニング（静的評価）

利益期待値のフィルタ:

```
gain_expected(cand) = round(V_i * (4*sqrt(P_i)/L_cand))
                    - move_cost_total
                    - penalty(future_block)
```

`penalty(future_block)` は「その配置が公園中央を塞ぐ度合い」で、`sum(距離重み * 芝生密度)` の簡易ヒューリスティック。

上位 Cand=20〜40 を Beam へ。

---

## 4. Beam Search 部分

### 4.1 なぜ「1 手 Flat Beam」か

- グループが 1000 個、各手の分岐が数十以上あるため、多段 Beam を張っても 2〜3 手先で状態数が発散する。
- ロールアウトの評価精度がはるかに効くため、「1 手だけ広く」＋「先読みは MC」の二段構えが実効的。

### 4.2 実装

```cpp
struct BeamNode {
    Action act;      // 実行する行動（移動列 + 配置 or Skip）
    State  st;       // act 実行後の状態（deep copy）
    double static_score;
};

std::vector<BeamNode> beam;
for (auto& cand : candidates) {
    State s2 = base;
    apply(s2, cand);              // 移動 & 配置を反映
    double sc = static_eval(s2, cand);
    beam.push_back({cand, std::move(s2), sc});
}
// 静的スコアで top-K を残す
std::partial_sort(beam.begin(), beam.begin()+K, beam.end(),
    [](auto& a, auto& b){ return a.static_score > b.static_score; });
beam.resize(K);
```

**静的評価関数 static_eval**

```
static_eval(s, cand) =
    money_delta_now                              // 今支払われる/引かれる金
  + λ1 * expected_pay(cand)                      // 退去時の見込み
  - λ2 * blocked_area_penalty(s, cand)           // 未来の芝生断片化
  - λ3 * congestion_penalty(s)                   // 直近時刻に他Gが来るか
```

`λ1=1.0, λ2=0.3, λ3=0.2` を初期値、後述の Simulated Annealing でオンライン調整。

---

## 5. Monte Carlo Rollout

### 5.1 Rollout ポリシー

Beam の各 top-K 状態からロールアウトする。ロールアウト内では **正確性より速度**：

```
rollout(s0, seed):
    s = clone(s0)
    while s.next_i < M:
        g = groups[s.next_i]
        // 到着処理: 期限切れ退去（leave_pq を進める）
        process_leaves_until(s, g.S)

        // 高速 Greedy 配置
        act = fast_greedy_place(s, g, rng)
        apply_no_snapshot(s, act)
        s.next_i++
    process_leaves_until(s, INF)
    return s.money
```

### 5.2 fast_greedy_place

- 候補生成は「正方形/長方形の shuffle 済みリストを上から数個」だけ（20 個以内）。
- 「初手で置けたら即決」＋ε-greedy で 10% はランダムに別テンプレ選択。
- 移動は原則しない（速度優先）。ただし所持金が十分あり、かつ現グループの V が高く配置不能な場合のみ、V が最小の利用中グループを 1 つだけ空きスペースへ動かす。

### 5.3 実装上の高速化

1. **`owner` を `std::array<int16_t, 2500>`** で持ち、rollout 毎に memcpy で復元。
2. **芝生連結成分**を事前計算し、テンプレ配置時にはビットボード（`uint64_t[40]` = 50×50/64）で `and` チェック。
3. **rng は xorshift128+**（`std::mt19937` は遅い）。
4. **境界長 L の計算をテーブル化**: テンプレごとに L 定数を保持。BFS 成長時のみ差分計算。
5. **Rollout 中は退去精算のみ実施、コンパクト度計算は「そのグループが `C_min_L` を更新するときだけ」**。
6. **Rollout 時間予算**を計測ベースで動的に調整（後述）。

### 5.4 集約

各 Beam ノード k について R 回ロールアウトの平均を取る:

```
score(k) = (1/R) * Σ_r rollout(beam[k].st, seed_r)
```

ばらつきが大きいなら UCB1 で予算を偏らせる：

```
UCB(k) = mean_k + c * sqrt(log(N_total) / n_k)
```

---

## 6. 時間予算

制限時間は AHC 系の相場で 2 秒（実測要確認）。1 グループあたりに使える時間:

```
budget_per_group ≈ (T_total - safety) / M
                 ≈ (2000 - 100) / 1000 ≈ 1.9 ms
```

これは厳しいので:

- **序盤（i < 100）と要衝グループ（V が高い / P が大きい）に予算を厚く配分**。
- **簡単な状況（利用中が少ない・空きが多い）は Beam=1, R=0 の即決 Greedy**。
- **`std::chrono::steady_clock` で残時間を測り、超過寸前で MC を中断**。

Rollout 1 回の目標時間: **50〜200 µs**。M=1000 のフル rollout を 200 µs で回すには、1 グループ処理を 200 ns 程度に抑える必要がある。実現は厳しいので、**シミュレート先を “残り min(M-i, 200) グループ” に truncate** して重み付き外挿:

```
projected_final = current_money + rollout_gain(next 200 groups) * (M-i)/200
```

これで rollout コストを O(200) に固定できる。

---

## 7. MCTS 検討

### 7.1 なぜ本問題で MCTS はしんどいか

1. **分岐数が巨大**: 各 i で有効候補が数百（BFS成長を含めれば数千）。UCB を回すには最低でも「各枝を 1 回」試すことになり、深さ 2 でも爆発。
2. **確定的**: 環境ランダム性が無いので “root からの選択木” を無限に固定でき、逆に MCTS 特有の期待値平均化の恩恵が薄い。
3. **深さ = M = 1000**: 木の深さが極端に大きく、UCB での分岐選択の分散が減衰しない。
4. **静的評価がそこそこ効く**: L の計算だけで大体の良し悪しが分かるため、「木で探るより評価関数で刈る」ほうが割が良い。

### 7.2 分岐数の見積り

- 純粋な配置候補: BFS成長を許すと `O(芝生マス数 × 平均P) ≈ 2500 × 20 ≈ 5×10^4`。テンプレのみでも各テンプレ×位置 ≈ `(50×50)×10 = 2.5×10^4`。
- 事前移動を含めると `× (利用中グループ数)^k` で爆発。

### 7.3 木を限定するなら

- 深さは **せいぜい 2〜3**。
- 各ノードの子は **静的評価で 5 個以下** に制限。
- 各葉から MC rollout。
- Progressive Widening: `#children(n) = ceil(C * n(n)^α)`、`α ≈ 0.3`, `C ≈ 2`。

### 7.4 Beam Search との比較

|観点|Beam+MC|MCTS|
|---|---|---|
|実装難度|低〜中|高|
|決定的問題への相性|◎|△|
|分岐爆発耐性|◎（枝を top-K で切る）|△（PW が必要）|
|深い先読み|△（rollout に依存）|○（tree で伸ばせる）|
|1手あたり時間の均一性|◎|△|
|チューニング難度|低|高|

**結論**: 本問題は Beam Search + MC Rollout の方が明確に有利。MCTS を採るなら深さ 2 + PW + rollout 併用の「Nested MC」構成が最低ライン。

---

## 8. 局所探索の併用

### 8.1 使いどころ

|手法|いつ|何を動かすか|
|---|---|---|
|**Hill Climbing**|Beam 決定後・出力直前|選択候補の配置形状を近傍で微調整（±1 行/列ずらす、L が下がる方向へ）|
|**Simulated Annealing**|大きな時間余裕がある序盤|「今利用中のグループを 1〜2 個移動して new group を良い形で入れる」複合手|
|**Random shuffle**|手詰まり時（配置不能検出）|V の小さい利用中グループを候補として動かす|
|**Group Swap**|Beam の候補生成の一部|2 グループの領域を入れ替えて双方の L を減らす（コストは 2 グループ分の移動費）|
|**Config tuning (offline SA)**|ローカルテストで λ1, λ2, λ3 を SA|–|

### 8.2 タイミング指針

- **候補生成中**: HC で L を極小化（微調整）。
- **Beam 決定直前**: 移動を伴う候補に対して SA を軽く回して移動先を最適化。
- **出力直後・次グループ到着前の隙間時間**: 未来を見た swap 探索（オフスレッド）。

### 8.3 Hill Climbing 擬似コード

```cpp
Placement local_hc(Placement p, int iter=30) {
    int best_L = compute_L(p);
    for (int t=0; t<iter; ++t) {
        Placement q = perturb(p);           // 1 マスの入替 or 平行移動
        if (!is_valid(q)) continue;
        int L = compute_L(q);
        if (L < best_L) { p = q; best_L = L; }
    }
    return p;
}
```

`perturb`:

- 領域内から端マス 1 つを取り除き、境界の空きマスから 1 つ追加。
- 領域全体を上下左右に 1 マス平行移動。
- ±1 行/列の rotate。

---

## 9. C++ 実装スケルトン

```cpp
#include <bits/stdc++.h>
using namespace std;
using ll = long long;

static constexpr int N_MAX = 50;
static int N, M;
static double R;
static array<uint8_t, N_MAX*N_MAX> park; // 1=grass, 0=pond

struct Group { int id,S,T,P,V; };
struct Placement { vector<int> cells; int L; };

struct State {
    array<int16_t, N_MAX*N_MAX> owner{};   // -1 default via memset
    struct Live { int id, P, V, T, cur_L, worst_L; vector<int> cells; };
    vector<Live> live;
    unordered_map<int,int> id2slot;
    ll money = 0;
    int next_i = 0;

    void init() { owner.fill(-1); }

    void settle_leaves_until(int tnow, vector<Group>& gs) {
        // 利用中で T_j <= tnow の全員を退去精算
        for (auto it = live.begin(); it != live.end(); ) {
            if (it->T <= tnow) {
                double C = 4.0*sqrt((double)it->P) / (double)it->worst_L;
                money += (ll)floor((double)it->V * C + 0.5);
                for (int c : it->cells) owner[c] = -1;
                id2slot.erase(it->id);
                it = live.erase(it);
            } else ++it;
        }
    }

    void place(const Group& g, const Placement& pl) {
        Live L{g.id, g.P, g.V, g.T, pl.L, pl.L, pl.cells};
        for (int c : pl.cells) owner[c] = (int16_t)g.id;
        id2slot[g.id] = live.size();
        live.push_back(std::move(L));
    }

    void move(int gid, const Placement& pl) {
        Live& L = live[id2slot[gid]];
        for (int c : L.cells) owner[c] = -1;
        for (int c : pl.cells) owner[c] = (int16_t)gid;
        L.cells = pl.cells;
        L.cur_L = pl.L;
        L.worst_L = max(L.worst_L, pl.L);
        money -= max<ll>((ll)floor(L.V * R + 0.5), 1);
    }
};

// ---------- 候補生成 ----------
vector<Placement> gen_rect_candidates(const State& s, int P);
vector<Placement> gen_bfs_candidates(const State& s, int P);

// ---------- 静的評価 ----------
double static_eval(const State& after, const Group& g, const Placement& pl,
                   ll move_cost);

// ---------- 高速ロールアウト ----------
struct Xorshift { uint64_t s; uint64_t next(){ s^=s<<7; s^=s>>9; return s; } };

ll rollout(State s, vector<Group>& gs, Xorshift& rng, int horizon=200) {
    int end_i = min(M, s.next_i + horizon);
    for (; s.next_i < end_i; ++s.next_i) {
        auto& g = gs[s.next_i];
        s.settle_leaves_until(g.S, gs);
        auto cands = gen_rect_candidates(s, g.P);
        if (cands.empty() || (rng.next()%10)==0) {
            // Skip
        } else {
            // 上位数個からランダムに 1 つ
            int k = min<int>(cands.size(), 5);
            auto& pl = cands[rng.next()%k];
            s.place(g, pl);
        }
    }
    s.settle_leaves_until(INT_MAX, gs);
    // 端数補正（打ち切りぶんの外挿）
    ll est = s.money;
    return est;
}

// ---------- Beam + MC 統合 ----------
struct BeamCand {
    vector<pair<int,Placement>> moves; // (gid, new placement)
    optional<Placement> place_new;     // nullopt = Skip
    State after;
    double stat;
    double mc_mean = 0;
    int    mc_n    = 0;
};

Placement decide_action(State& cur, vector<Group>& gs, int i,
                        double time_left) {
    auto& g = gs[i];

    // 1) raw candidates
    vector<BeamCand> C;
    for (auto& pl : gen_rect_candidates(cur, g.P)) {
        State s2 = cur;
        s2.place(g, pl);
        s2.next_i = i+1;
        double sc = static_eval(s2, g, pl, 0);
        C.push_back({{}, pl, std::move(s2), sc});
    }
    // BFS 候補、事前移動候補も同様に追加 ...

    // Skip
    {
        State s2 = cur; s2.next_i = i+1;
        C.push_back({{}, nullopt, std::move(s2), static_eval_skip(cur, g)});
    }

    // 2) top-K
    int K = min<int>(C.size(), 6);
    partial_sort(C.begin(), C.begin()+K, C.end(),
        [](auto& a, auto& b){ return a.stat > b.stat; });
    C.resize(K);

    // 3) MC rollout
    static Xorshift rng{0xdeadbeef};
    double t0 = now_ms();
    double budget = min(time_left * 0.4, 3.0); // ms
    int total_rolls = 0;
    while (now_ms() - t0 < budget) {
        // UCB1 で 1 本選ぶ
        int pick = pick_ucb(C, total_rolls);
        ll v = rollout(C[pick].after, gs, rng);
        C[pick].mc_mean += (v - C[pick].mc_mean)/(++C[pick].mc_n);
        ++total_rolls;
    }

    // 4) 決定
    auto best = max_element(C.begin(), C.end(),
        [](auto& a, auto& b){ return a.mc_mean < b.mc_mean; });

    // 5) 出力：先に移動、その後 Yes/No
    return best->place_new.value_or(Placement{});  // 呼び出し側で出力
}
```

---

## 10. 計算量見積り

- 候補生成: 矩形 O(N² × テンプレ数) ≒ 25000 × 10 = 2.5×10⁵ / group。
- 静的評価: 候補ごとに `L`, 期待利益、ペナルティで O(P)。全体 O(Cand × P) ≈ 8000。
- Beam top-K: `partial_sort` O(Cand log K) ≈ 数百ops。
- MC Rollout 1 本: 打ち切り horizon=200 として `O(200 × (候補生成コスト + 配置コスト))`。候補生成を「先頭 5 テンプレのみ試す」に絞ると 1 rollout ≈ 30〜100 µs。
- 1 グループ全体: 3 ms 目安（K=6, R=20〜30 本）。M=1000 で 3 秒 → **horizon と R を序盤で厚く、後半で薄く**して総 2 秒に収める。

---

## 11. 実装フェーズ推奨順

1. **基本 Greedy (矩形候補 + 静的評価のみ) を提出**して baseline を取る。
2. **Beam(K=1) + Rollout(R=8, horizon=150)** を載せて比較。
3. **候補に BFS 成長と Skip を追加**。
4. **事前移動候補**を追加（V 小＋高収益の新規のみ）。
5. **Hill Climbing 微調整**を出力直前に挿入。
6. **時間配分の適応制御**（残時間ベース）。
7. **λ の SA 自動チューニング** をローカルで実施し提出。

---

## 12. まとめ

- **本問題の勘所**は「未来のグループ到着スケジュールを踏まえた、L (境界長) を小さく保つ配置」と「時々の再配置の損得判断」。
- MCTS は分岐数と決定性の観点で不利。**Beam(1 手広く) + Monte Carlo Rollout(残りを高速Greedyで外挿) + 局所改善 (HC/SA)** が最も費用対効果が高い。
- 実装は「候補生成 → 静的評価 → top-K → MC → 決定」の 5 段を明確に分離しておくと、各段で個別に高速化・チューニングできて改善サイクルが回しやすい。

必要であれば、`gen_rect_candidates` / `gen_bfs_candidates` / `static_eval` の詳細実装、あるいは実測に合わせた時間配分の自動制御コードを次のステップで書き起こします。